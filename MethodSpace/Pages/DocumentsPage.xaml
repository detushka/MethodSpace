<Page x:Class="MethodSpace.Pages.DocumentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Методическое обеспечение">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Методическое обеспечение"
                   Style="{StaticResource PageTitleStyle}"/>

        <Border Grid.Row="1"
                Style="{StaticResource FilterPanelStyle}">
            <StackPanel>
                <TextBlock Text="Фильтр по типу документов"
                           Style="{StaticResource SectionTitleStyle}"
                           Margin="0,0,0,10"/>
                <WrapPanel>
                    <RadioButton x:Name="AllDocs" Content="Все" GroupName="DocType" IsChecked="True"
                                 Checked="Filter_Changed"/>
                    <RadioButton x:Name="WorkingPrograms" Content="Рабочие программы" GroupName="DocType"
                                 Checked="Filter_Changed"/>
                    <RadioButton x:Name="MethodicalRecs" Content="Методические рекомендации" GroupName="DocType"
                                 Checked="Filter_Changed"/>
                    <RadioButton x:Name="Regulations" Content="Нормативные документы" GroupName="DocType"
                                 Checked="Filter_Changed"/>
                    <RadioButton x:Name="Instructions" Content="Инструкции" GroupName="DocType"
                                 Checked="Filter_Changed"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Style="{StaticResource FilterPanelStyle}"
                Padding="8"
                Margin="0,0,0,10">
            <ListView x:Name="DocumentsList">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Название" Width="300" DisplayMemberBinding="{Binding Title}"/>
                        <GridViewColumn Header="Тип" Width="170" DisplayMemberBinding="{Binding DocumentType}"/>
                        <GridViewColumn Header="Дисциплина" Width="200" DisplayMemberBinding="{Binding DisciplineName}"/>
                        <GridViewColumn Header="Дата загрузки" Width="130">
                            <GridViewColumn.DisplayMemberBinding>
                                <Binding Path="UploadDate" StringFormat="dd.MM.yyyy"/>
                            </GridViewColumn.DisplayMemberBinding>
                        </GridViewColumn>
                        <GridViewColumn Header="Скачивания" Width="90" DisplayMemberBinding="{Binding DownloadsCount}"/>
                        <GridViewColumn Header="Действия" Width="110">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Скачать"
                                            Tag="{Binding DocumentID}"
                                            Click="DownloadButton_Click"
                                            Style="{StaticResource ActionButtonStyle}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <TextBlock x:Name="StatusText"
                   Grid.Row="3"
                   Style="{StaticResource HintTextStyle}"
                   HorizontalAlignment="Right"/>
    </Grid>
</Page>
