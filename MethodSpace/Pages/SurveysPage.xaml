<Page x:Class="MethodSpace.Pages.SurveysPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Опросы"
      Loaded="Page_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Активные опросы"
                   Style="{StaticResource PageTitleStyle}"/>

        <Border Grid.Row="1"
                Style="{StaticResource FilterPanelStyle}"
                Padding="8"
                Margin="0">
            <ListView x:Name="SurveysList"
                      SelectionChanged="SurveysList_SelectionChanged">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Название" Width="250" DisplayMemberBinding="{Binding Title}"/>
                        <GridViewColumn Header="Описание" Width="400" DisplayMemberBinding="{Binding Description}"/>
                        <GridViewColumn Header="Дата окончания" Width="150"
                                        DisplayMemberBinding="{Binding EndDate, StringFormat='dd.MM.yyyy'}"/>
                        <GridViewColumn Header="Действия" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Пройти"
                                            Tag="{Binding SurveyID}"
                                            Click="TakeSurvey_Click"
                                            Style="{StaticResource SuccessButtonStyle}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</Page>
