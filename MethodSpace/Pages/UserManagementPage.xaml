<Page x:Class="MethodSpace.Pages.UserManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Управление пользователями"
      Loaded="Page_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Управление пользователями"
                   Style="{StaticResource PageTitleStyle}"/>

        <Border Grid.Row="1"
                Style="{StaticResource FilterPanelStyle}">
            <StackPanel Orientation="Horizontal">
                <Button Content="Добавить пользователя" Click="AddUser_Click"
                        Style="{StaticResource SuccessButtonStyle}" Width="210" Height="38"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Style="{StaticResource FilterPanelStyle}"
                Padding="8"
                Margin="0">
            <ListView x:Name="UsersList">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding UserID}"/>
                        <GridViewColumn Header="ФИО" Width="200" DisplayMemberBinding="{Binding FullName}"/>
                        <GridViewColumn Header="Email" Width="220" DisplayMemberBinding="{Binding Email}"/>
                        <GridViewColumn Header="Роль" Width="130" DisplayMemberBinding="{Binding RoleDisplay}"/>
                        <GridViewColumn Header="Статус" Width="110" DisplayMemberBinding="{Binding StatusDisplay}"/>
                        <GridViewColumn Header="Действия" Width="170">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Изменить" Tag="{Binding UserID}" Click="EditUser_Click"
                                                Width="82" Margin="0,0,6,0" Style="{StaticResource ActionButtonStyle}"/>
                                        <Button Content="{Binding ToggleLabel}" Tag="{Binding UserID}" Click="ToggleUserStatus_Click"
                                                Width="82" Style="{StaticResource ActionButtonStyle}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</Page>
